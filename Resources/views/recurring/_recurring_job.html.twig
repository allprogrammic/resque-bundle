<tr>
    <td>
        <a href="{{ path('resque_recurring_history', {id: id}) }}">
            {{ job.name }}
        </a>
    </td>
    <td>
        {{ job.description }}
    </td>
    <td>
        <code>{{ job.class }}</code>
    </td>
    <td>
        <a class="label label-info" href="#">
            {{ job.queue }}
        </a>
    </td>
    <td>
        {{ job.cron }}
    </td>
    <td>
        <a class="btn btn-default" href="{{ path('resque_recurring_start', {id: id}) }}" title="Start now">
            <i class="glyphicon glyphicon-repeat"></i>
        </a>
        <a class="btn btn-default" href="{{ path('resque_recurring_history', {id: id}) }}" title="History">
            <i class="glyphicon glyphicon-list-alt"></i>
        </a>
        <a class="btn btn-default" href="{{ path('resque_recurring_update', {id: id}) }}" title="Update">
            <i class="glyphicon glyphicon-pencil"></i>
        </a>
        <a class="btn btn-danger" href="{{ path('resque_recurring_remove', {id: id}) }}" title="Remove">
            <i class="glyphicon glyphicon-trash"></i>
        </a>
    </td>
</tr>