<tr>
    <td>
        <a href="{{ path('resque_recurring_history', {id: id}) }}">
            {{ job.name }}
        </a>
    </td>
    <td>
        {{ job.description }}
    </td>
    <td>
        <code>{{ job.class }}</code>
    </td>
    <td>
        <a class="label label-info" href="#">
            {{ job.queue }}
        </a>
    </td>
    <td>
        {{ job.cron }}
    </td>
    <td>
        <a class="btn btn-default" href="{{ path('resque_recurring_history', {id: id}) }}">
            History
        </a>
        <a class="btn btn-default" href="{{ path('resque_recurring_update', {id: id}) }}">
            Update
        </a>
        <a class="btn btn-danger" href="{{ path('resque_recurring_remove', {id: id}) }}">
            Remove
        </a>
    </td>
</tr>